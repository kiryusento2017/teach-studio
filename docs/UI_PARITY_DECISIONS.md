# 界面一致性 · 决策台账

云端版（本项目）跟本地版 `pdf_to_word` 的界面/交互对照，逐条记**改还是不改、
为什么**。差异清单本身不在这儿，在报告里：

> https://claude.ai/code/artifact/0f0ff4df-0069-4e45-8d93-7ca5578a82d2
> 126 处差异（视觉 76 · 交互 36 · 漏搬 9 · 外壳 5 · 文案 12），每条带双侧行号。

**这份文件只记结论。** 编号跟报告里的一致，想知道某条具体差在哪，拿编号回报告查。

## 怎么用

- **动手前**先看这儿有没有记过。记过「不改」的别再改回来，那是决定不是遗漏。
- **改完**把状态从 `☐` 划成 `☑`，并写上提交号。
- **有新差异**追加到对应类，别新开文件。

## 状态图例

| 记号 | 意思 |
|---|---|
| `❓` | 还没定，等小蔡拍板 |
| `☐` | 定了要改，还没动手 |
| `☑` | 改完了 |
| `—` | 定了不改，本条到此为止 |

---

## 总账

| 类 | 条数 | 处置 | 定了没 |
|---|---|---|---|
| A 功能坏了 | 9 | 必须改 | ☑ 已改完 2026-09-09 |
| B 许可声明 | 1 | 必须改 | ☑ 已改完 2026-09-09 |
| C 待转清单 | 8 | 改 | ☑ 已改完 2026-09-09 |
| D 死路加出口 | 4 | 改 | ☑ 全部完成 2026-09-09 |
| E 有取舍的 | 14 | **全做** | ☑ 全部完成 2026-09-09 |
| F 云端更对 | 5 | **保留云端做法** | — 已拍板，到此为止 |
| G 本该不一样 | 12 | 不改 | ☑ 无争议 |
| H 纯风格措辞 | 73 | 不改 / 顺手 | ☑ 无争议 |

**一句话**：两边 CSS 是同一份（178 行只差 3 处文字），126 处差异<b>没有一条源于样式表</b>，
全出在 `pages.js` 怎么用这些类。所以修的时候不用碰 `index.html`。

---

## 拍板记录 · 2026-09-09

小蔡逐条定了，本台账的处置到此全部有主：

| 类 | 决定 | 原话 |
|---|---|---|
| A / B | **全改** | 「一定要改的你先全改了吧」→ 已完成 |
| C | **现在就改** | 待转清单 8 条 |
| D | D1/D3/D4 随 A 完成；D2 单列 | |
| E1–E11 | **按我给的倾向执行**，改完逐条汇报，有异议随时叫停 | |
| E12 / E13 / E14 | **三个大件全做** | 报告页每份都列 · 检查更新恢复整屏 · 重绘策略照搬本地 |
| F | **保留云端做法** | 这 5 条按「向本地看齐」改等于把 bug 抄回来 |
| G / H | 不改 | 无争议 |

**执行顺序**（按性价比排，不是按严重度）：
C（一屏吃掉 8 条高严重度）→ E1–E11（多是几行的事）→ E12 报告页 →
E14 重绘策略（涉核心渲染，单独做单独验）→ E13 检查更新整屏（最大，约 190 行）。

**C 类顺带解决一个遗留**：改成「整行可点 + checkbox 设 `pointer-events:none`」之后，
A2 补的那次 `render()` 就不再抢焦点了 —— 当时留的那个代价到这一步自动消失。

---

## A · 功能坏了（9 条）

跟「像不像本地版」无关，是云端版单独存在的缺陷。**提案：全改。**

| # | 症状 | 根因 | 改法 | 状态 |
|---|---|---|---|---|
| A1 | 转完之后拖文件进来，不问一句直接开转、直接扣额度 | `actions.js:188` 判忙用 `st.task` 真值，done 快照也算忙 → 进待办 → 体检回来发现已 done → `promotePending()` → `start()` | 换回本地版的 `isRunning \|\| starting` | ☑ |
| A2 | 勾选之后底栏「选中 N 份」和「开始转换」禁用态都不更新 | `app.js:153` 注释明写「只改选中状态，不重画」；取消勾光后按钮仍可点，点了 `actions.js:462` 静默 return | change 处理里补一次 `render()` | ☑ |
| A3 | 后台起不来时显示的是「请填 token」，且无重试出口 | `app.js:213-217` 把错误塞进 `st.err` 并置 `ready=true`，落到 token 引导屏；没有 `reload` 动作 | 补「连不上后台服务」专屏 + 重试按钮（照搬本地 `pages.js:1334-1342`） | ☑ |
| A4 | 拦截屏一个按钮都没有 | `pages.js:85-90` 的 `gateView` 只有标题 + 一段字 | 加「我挪好了，重新检查」+「退出」两个按钮 | ☑ 按钮已加；**目录原文没做** |
| A5 | 启动瞬间空白窗口，写好的「正在启动…」是死代码 | `app.js:200-206` 直接进 `getPort().then()`，首帧要等 `/api/env` | 照本地 `app.js:455`，`getPort()` 之前先 `render()` 垫一帧 | ☑ |
| A6 | 拖错东西进来，高亮赖着不走；拖没 PDF 的文件夹零反馈 | `app.js:195` 只有 `if (paths.length)`，**没有 else** | 补 `else render()`；扫完没 PDF 时给「这里面没有找到 PDF」 | ☑ |
| A7 | 历史页/设置页里拖文件完全没反应 | 云端把历史/设置做成独立 `page`，`page !== 'main'` 从「只拦下模型屏」变成「拦掉一半界面」 | 放宽判据，只拦真正不该收的屏 | ☑ |
| A8 | 点「开始转换」那一瞬拖进来的文件永久停在「未处理」 | 同 A1：用 `st.task` 代替了 `starting`，那一刻 `st.task` 还是 null | 跟 A1 一起修 | ☑ |
| A9 | 「+ 添加」那一栏敲回车不生效 | `actions.js:348` 写 `editSlot='new'`，`app.js:159` 判 `editSlot >= 0`，而 `'new' >= 0` 是 false | 统一判据 | ☑ |

**注**：A1 和 A8 是同一处代码（`actions.js:188`）的两个症状，一起修。
A1 是唯一一条会**擅自消耗 MinerU 额度**的，建议排最前面。

### 执行记录 2026-09-09

九条全改完，前端 **67 条**、后端 115 条全绿。改动落在
`app/renderer/{app.js,actions.js,pages.js}`，四个文件行尾仍是纯 LF。
`pages.js` 用到的 34 个 `data-act` 与 `actions.js` 定义的 34 个完全对上，没有漏接。

🔴 **动了一条测试**：`体检回来时这批已经转完了，也要自己补一次晋升`。
它原先用 `st.task = { state: 'done' }` 开局，跟自己的注释（「扫的十几秒里
轮询才拿到 done」）矛盾 —— 锁住的其实是 A1 那个 bug 行为。改成 `running`
开局、调用 `addPaths` 后同步改 done，精确还原真实时序；另新增两条
（`转完之后再拖一份进来，不许自动开转`、`点开始那一瞬拖进来的，要进待办不能进清单`）
把修复钉死。**结果是加严，不是放宽**，测试从 65 条变成 67 条。

⚠️ **未真机验证**：所有结论来自代码与测试，没有把软件跑起来看过界面。
关于页是全新的一屏，务必打开看一眼。

---

## B · 许可声明（1 条）

| # | 事项 | 现状 | 提案 | 状态 |
|---|---|---|---|---|
| B1 | 界面上没有任何地方声明许可和第三方组件 | 云端版前端搜不到「关于」、搜不到 `LICENSES`。而仓库里有 35 KB 的 GPL LICENSE、`package.json` 写 `GPL-3.0-or-later`、运行时内置了 pandoc（GPL-2.0+）和微软 `MML2OMML.XSL` | 补关于页：版本 / 作者 / GPL / 组件许可表（按云端实际依赖重列成 11 项） | ☑ **没有 sha** |

**为什么这条不只是「不像」**：GPL 和 pandoc 都要求分发时保留许可信息，
而这软件已经打包发出去了（`teach-studio-Setup-v0.0.1.exe`）。
CLAUDE.md 第四条也记着 `MML2OMML.XSL` 是微软版权文件、只因为「发给几个熟人」才内置。

### 执行记录 2026-09-09

`pages.js` 加了 `LICENSES` 常量 + `aboutView`，注册成 `about` 页；
三处底栏都补了「关于」入口（顺带把 #17 的链接顺序变成 设置 · 历史 · 关于）；
`actions.js` 接上 `openAbout`。版本号取 `/api/env` 的 `version`。

**组件表按云端实际依赖重列，不是照抄本地版**（云端不跑本地模型，没有 PyTorch）：

| 组件 | 许可 | 组件 | 许可 |
|---|---|---|---|
| MinerU | Apache-2.0 | pydantic | MIT |
| pandoc | GPL-2.0+ | requests | Apache-2.0 |
| PyMuPDF | **AGPL-3.0** | Node.js | MIT |
| KaTeX | MIT | Electron | MIT |
| lxml | BSD-3-Clause | MML2OMML.XSL | © Microsoft |
| FastAPI | MIT | uvicorn | BSD-3-Clause |

🔴 **顺带发现，需要小蔡确认**：`PyMuPDF` 是 **AGPL-3.0**（`dist/requires.json`
里 `pymupdf 1.28.2`），比 GPL 更严 —— 通过网络提供服务也要求开源。
而本项目声明的是 `GPL-3.0-or-later`。这两者能不能这么搭，**我不下结论**，
建议找懂的人看一眼。这条跟界面无关，是查关于页时撞出来的。

---

## C · 待转清单（8 条）

**这一屏性价比最高**：8 条全是高/中严重度，而且是用得最多的一屏。
本地版对应代码在 `pdf_to_word/app/renderer/pages.js:1440-1466`，基本可以照搬。

| # | 事项 | 本地版 | 云端版现状 | 状态 |
|---|---|---|---|---|
| C1 | 表头行 | `.hd`：「文件（共 N 份）」+ 全选/全不选 +「页数」列头 `pg:1457-1462` | **`class="hd"` 全文零命中**——样式表里定义了却没用 | ☑ |
| C2 | 全选 / 全不选 | `selAll` / `selNone` `act:479-487` | 动作表和表头都没有，几十份只能一个个点 | ☑ |
| C3 | 整行可点选 | 整行 `data-act="toggle"`，checkbox `pointer-events:none` `pg:1447-1450` | 只有裸 checkbox 带 `data-check`，**点击目标缩成 13px** | ☑ |
| C4 | 默认勾选 | `!== false` → 拖进来默认全勾 `pg:1434` | `st.picked[x.path]` → **默认全不勾** | ☑ |
| C5 | 页数列对齐 | `width:52px;text-align:right` `pg:1453` | `.rt` 无固定宽，**页数跟着内容飘，行行对不齐** | ☑ |
| C6 | 未勾选调淡 | 文件名加 `f-dim` `pg:1451` | 勾不勾同色，看不出选没选 | ☑ |
| C7 | 无文字层用橙色 | `rt f-warn` `pg:1452` | `rt` 灰色，警告不像警告 | ☑ |
| C8 | 整份无文字层 | 单说「整份没有文字层」`pg:1439` | 无此分支，一律「第 X 页没有文字层」 | ☑ |

### 执行记录 2026-09-09

八条一次做完。`pages.js` 重写 `itemRow`（整行 `data-act="toggle"` + checkbox
`pointer-events:none` + 未选 `f-dim` + note 上 `f-warn` + 页数 `width:52px` 右对齐
+ 整份无文字层单独文案）、`mainPick` 加 `.hd` 表头（含全选/全不选，右侧留 30px
给每行的「移除」对齐）；`actions.js` 新增 `toggle`/`selAll`/`selNone`，
`picked` 全面改成**三态语义**（`!== false`，键不存在 = 选中）；
`app.js` 删掉整段 `change` 监听。

**顺带清掉一个遗留**：A2 当时补的 `render()` 会抢 checkbox 焦点，
现在点的是行、checkbox 不接事件，那个代价自动没了。

测试从 67 条加到 **71 条**：两条锁旧实现的改掉（`data-check` 属性名 →
「整行可点」行为；`picked={}` 在三态下是全选不是全不选），另补 4 条
（默认全勾能直接开始、表头与全选存在、页数固定列宽、没勾的调淡）。

**C3+C4 叠加的后果**：拖 20 份讲义进来要点 20 次 13px 的方块才能开始，
再叠上 A2（勾完底栏不刷新），点的过程中「选中 N 份」还一直不动。
这三条一起修才有意义，单修一条效果有限。

---

## D · 死路加出口（4 条）

用户走到这儿会卡住，屏上没有任何可点的东西。

| # | 事项 | 云端版现状 | 提案 | 状态 |
|---|---|---|---|---|
| D1 | 拦截屏（目录不可写 / 安装包不完整） | 一屏纯文字，**零个按钮** `pg:85-90` | 见 A4（同一处） | ☑ |
| D2 | 更新 need_full 分支 | 只有红点 + 错误文本，没有出路 `pg:259-261` | 已随 E13 做完：整屏 + 摆出地址 + 「复制下载页地址」primary + 一句「token 和转换历史都在里面，不会丢」 | ☑ |
| D3 | 连不上后台 | 见 A3 | 见 A3（同一处） | ☑ |
| D4 | 扫完没找到 PDF | 全程零反馈 | 补 `st.err = '这里面没有找到 PDF'`（本地 `act:87`） | ☑ |

**D2 有个坑**：本地版这里用 `openReleases` 弹浏览器，而云端版 2026-09-08 主动
拆掉了 `shell.openExternal` 和域名白名单（`main.js:347-355` 有注释）。
所以不能照搬，得改成 `copy-text` 复制下载页地址——跟设置页里「复制地址」一个路子。


---

## E · 有取舍的（14 条）· ☐ **已拍板：全做**

2026-09-09 小蔡定了：**E1–E11 按我给的倾向执行，E12/E13/E14 三个大件全做。**

### 执行记录 · E1–E11 已全部落地

| 条目 | 做了什么 |
|---|---|
| E1 / E6 | **不动**（底栏 token 用量、历史行公式计数），云端做法更贴云端 |
| E2 | 「停止」从顶栏挪回底栏；顶栏只留「正在转第 X / Y 份」和那句免责 |
| E3 | 底栏链接变成 设置 · 历史 · 关于（随 B1 完成） |
| E4 | 统计回底栏，「N 成 / M 份」→「全部完成 N 份」/「成功 X 份 · **失败 Y 份**（红）」 |
| E5 | 报告页底栏加「复制」+「这份报告只在这儿看，不会存成文件」。**走 `window.api.copyText`**，不能用 `navigator.clipboard`（file:// 下用不了） |
| E7 | **小改**：「320 毫秒」→「响应 320ms」 |
| E8 | 「再转一批」不清列表：失败的自动勾上、成功的取消勾选、**没转过的一根手指都不动** |
| E9 | `fetch` 全部套上 `AbortController`，默认 30 秒；**体检单独放宽到 300 秒**（扫大文件夹本来就慢） |
| E10 | 历史页空态给标题层级 + 副标题；补「共 N 份 · 鼠标停在失败那行上能看完整报错」 |
| E11 | 底栏加「日志」（转完也留着），新增日志屏；与报告互斥 |

🔴 **E7 我修正了自己的判断**：原先跟报告一样说云端「拿响应延迟冒充速度」。
细看之后 —— 云端那一列的语境是「各条线路怎么样」，**并没有「下载速度」这个列头**
（因为 #69 的表头也没搬），算不上冒充。真正的问题只是「320 毫秒」没说清是什么，
所以只加了两个字，没有照本地版重做整张表。

测试从 71 条加到 **77 条**：两条断言旧文案的跟着更新（`1 成 / 1 份`、`还没转过东西`），
新增 6 条（停止在底栏、失败数标红、历史提示语、再转一批三重断言、日志屏、日志报告互斥）。

### E-1 到 E-4：我给了倾向，你不反对就按这个来

| # | 争议 | 本地版 | 云端版 | 我的建议 | 定 |
|---|---|---|---|---|---|
| E1 (#16) | 底栏左侧放什么 | 环境自检「显卡 ✓ · 公式 ✓」 | 「N 个 token · 今天已用 X 页」 | **保留云端**。云端没显卡没模型可报，token 用量才是它该说的事 || — 保留云端 |
| E2 (#22) | 「停止」放哪 | 底部状态栏右侧 | 顶部工具条右端 | **改回底栏**。同一个动作两个软件放两处，是搬漏了不是设计 || ☑ |
| E3 (#17) | 底栏链接顺序 | 历史 · 关于 | 设置 · 历史 | **已顺带改成 设置 · 历史 · 关于**（B1 补关于页时一起做的） | ☑ |
| E4 (#28/29) | 完成统计 | 底栏「全部完成 N 份 · 用时 X」 | 顶栏灰字「N 成 / M 份」 | **改回本地版**。「3 成 / 3 份」容易读成「三成」 || ☑ |
| E5 (#44) | 报告能不能复制 | 底栏「复制」+ 一句「只在这儿看，不会存成文件」 | 没有 | **改**。报告不落盘，不能复制等于看完就没了 || ☑ |
| E6 (#52) | 历史行右侧显示什么 | 耗时 | 公式 / 表 / 图 计数 | **保留云端**。云端耗时基本等于网速，没参考价值 || — 保留云端 |
| E7 (#70) | 线路那一列放什么 | 只放实测字节率，没测显「—」，**明文拒绝拿响应延迟冒充速度** | 直接显示「X 毫秒（用的这条）」 | **至少改这条**。这是原则性偏离，不是排版 || ☑ 小改 |
| E8 (8.3) | 「再转一批」干什么 | 不清列表，失败的自动勾上 = 一键重试 | 全清空，要重新拖 | **改回本地版**。同名按钮行为相反，而且失败的还得重拖一次 || ☑ |
| E9 (7.5) | 请求超时兜底 | 仅 `exportDiag` 有 30 秒 | **全项目一个都没有** | **改**。后端挂住会让 `starting`/`tokenBusy`/`updBusy` 永久卡忙态，只能重启软件 || ☑ |
| E10 (#50/49) | 历史页空态和提示语 | 有标题层级 + 「鼠标停在失败那行能看完整报错」 | 单行灰字，无提示 | **改**。顺手的事 || ☑ |
| E11 (#23) | 转换中能不能看日志 | 底栏「日志」按钮 + 日志屏 | 没有入口（**后端照样在返回 `lines`**） | **改**。数据已经在手上，只差一个按钮一个屏 || ☑ |

### E12–E14：三个大件，小蔡定了**全做**

| # | 争议 | 选项 A（本地版） | 选项 B（云端现状） | 我的看法 | 定 |
|---|---|---|---|---|---|
| E12✓ (#38/39) | 报告列多少 | **每一份都列**，含成功且干净的；每份带「N 页 · 公式 a/b · 表 c · 图 d」+ 落盘路径 | 只列有问题的份，无统计无路径 | B 更短，但「没列出来」和「没问题」分不清 —— 用户没法确认软件到底看没看那一份。A 更啰嗦但可核对。**你更在意「一眼扫完」还是「能核对」？** | ☑ |
| E13✓ (#58) | 检查更新的形态 | 独立整屏，每个分支都是大标题 + 说明 + 按钮组（约 190 行） | 设置页里的一个区块，全压成单行 | **工作量最大的一条。** 但它藏在设置页里，一年看不了几次 —— 花大力气改性价比存疑 | ☑ |
| E14✓ (1.1/1.3/1.4) | 重绘策略 | 局部 patch 几个数字 + 反向排除表 + 「抓手不在就什么都不做」 | 签名相同就整个不刷，不同就整页 `innerHTML` 重来；排除表只剩 `elapsed` 一项 | 本地版为此**专门修过两个卡顿场景**（日志字段一变就整页重绘、非转换屏也每秒重绘），云端原样存在。改动量中等，但涉及核心渲染路径，**风险比前面几条高** | ☐ |

---

## F · 云端版做得更对（5 条）· — **已拍板：保留云端做法，不改**

🔴 **这几条如果按「向本地版看齐」改，是把 bug 抄回来。**
我的建议是**保留云端做法**，甚至反过来把本地版 `pdf_to_word` 改了（那是另一个项目，得单独开工）。

| # | 云端做法 | 本地版的毛病 | 建议 | 定 |
|---|---|---|---|---|
| F1 (7.1) | `httpGet` 检查 `r.ok` 并挂 `e.status` | 本地 `get()` 不判，错误体被当成正常数据 resolve | 保留云端 | — |
| F2 (2.6) | 任务 404 真能停轮询，并说「这个任务已经不在了，重新开始吧」 | 本地那段是**死代码**（因为 F1），每秒空转 404，界面永久停在旧状态且一言不发 | 保留云端 | — |
| F3 (7.2) | 统一 `errBar` 挂每屏顶部，带「知道了」可关 | 本地错误只在两屏渲染、**不能关**，转换屏出错一个字都看不到 | 保留云端 | — |
| F4 (4.2) | handler 找不到时弹「这个按钮没接上：xxx」 | 本地静默吞掉，接线漏了表现为「点了没反应」 | 保留云端 | — |
| F5 (6.5) | 每行一个「移除」 | 本地只有「移除全部」，想去掉一份得全清重拖 | 保留云端 | — |

**附带**：F4 这条今天派上用场了 —— A4/A3/B1 新增了 `reload`/`quit`/`openAbout` 三个按钮，
要是漏接了动作，点下去会当场弹出来，而不是默默没反应。

---

## G · 本来就该不一样（12 条）· 不改

无争议，记录在案免得以后有人「顺手改回去」。

| 组 | 内容 | 为什么不算偏离 |
|---|---|---|
| 产品身份 | `name` / `version` / `description` 三项 | 两个软件本来就不是一个 |
| token 相关 | `tokenView` / `tokenEditRow` / `tokenRow` / `guideText` / 设置页 | 本地版没有 token 概念 |
| 右键菜单 | 云端有剪切/复制/粘贴/全选 | 为粘贴 token 加的，本地版没有输入框要粘 |
| 不弹浏览器 | 云端拆掉 `open-url` + `shell.openExternal` + 域名白名单，改 `copy-text` | 2026-09-08 的主动决策，`main.js:347-355` 有完整理由 |
| 跨平台 | 云端 Python 候选路径多 4 条 macOS/Linux | 多出来的兼容性，不影响 Windows |
| 用户数据目录 | 云端多迁移 `logs` / `crashDumps` | 更彻底，不往系统盘塞东西 |
| 砍掉的屏 | 显卡不达标 / 装 GPU 运行库 / 装 C++ 运行库 / 下模型 / 环境检测整页 / 升级备份 / 进度条 / 倒计时 / 两轮识别阶段名 | 云端不跑本地推理，这些屏没有存在的理由 |

---

## H · 纯风格与措辞（73 条）· 不改 / 顺手

低严重度，单条都不值得专门开工。清单在报告里，这儿只记原则：

- **数值微差**（`max-width` 差 10px、圆点 `#d0d0d0` vs `#8c8c8c`、时间列 78px vs 86px）—— 不改
- **实现风格**（拖放绑 `window` vs `document`、`closest()` vs 手写 while、行 `title` 放文件名还是全路径）—— 不改
- **同义不同词 12 条**（默认/还原、暂不更新/算了、重新检查/再查一次、准备中/准备中…）—— **改的时候顺手统一，不专门开工**
- **箭头用法两边正好相反**（本地 `▾▴` 标展开收起、返回不加箭头；云端反过来）—— 同上，顺手统一

**要是你想一次把 H 类扫干净**，那是另一个任务，说一声我单独做。

---

## 挂起 · 想做但没做的（都因为超出授权范围）

| # | 事情 | 为什么没做 | 要动什么 |
|---|---|---|---|
| P1 | 拦截屏显示「到底哪个目录写不了」 | 后端 `server/main.py:97` 的 `writable` 是 `os.access(ROOT, os.W_OK)`，**布尔值，不带路径**。前端无中生有不了 | 后端 env 加一个 `root` 字段 |
| P2 | 关于页显示 sha | 同上，`/api/env` 只给 `version`（tag）。`version.json` 里其实有 sha | 后端 env 把 sha 一并带出来 |
| P3 | 关于页给个「复制项目地址」 | 云端不弹浏览器（2026-09-08 的决定），想去仓库只能手抄。设置页已有 `copyTokenUrl` 可参照 | 前端加一个动作 |
| P4 | 「没找到 PDF」的判据 | 照搬本地版的 `!st.items.length`，清单里已有文件时再拖空文件夹仍不吭声。**两边同病** | 改成比对扫描前后的数量 |
| P5 | D2 更新 need_full 的出路按钮 | 我标的是「建议改」不是「一定要改」，没授权 | 加按钮 + 改成复制地址（云端不弹浏览器） |
| P6 | PyMuPDF 的 AGPL-3.0 与项目 GPL-3.0 是否相容 | **不是代码问题，是许可问题**，我不下结论 | 找懂的人看 |


---

## 收尾 · 2026-09-09

**八类全部处置完毕**，A/B/C/D/E 共 36 条改完，F 保留云端做法，G/H 不改。

### 最后三件（E12 / E14 / E13）

| 条 | 做了什么 |
|---|---|
| E12 | 报告页照本地版重写：每份都列、成功的给「N 页 · 公式 a/b · 表 c · 图 d」+ 落盘路径、扫描页截前 12 个 +「这几页最该核对」。后端字段本来就全（`convert.py:43-44`），没动后端 |
| E14 | 重绘三道闸：①签名没变不动 ②当前屏不显示任务就不画（历史/设置/关于）③结构没变只 patch `convnow`/`convline` 两处 textContent。`convSig` 里 lines 只比最后一行（日志屏开着才比全部） |
| E13 | 更新从设置页的区块变回**独立整屏**，10 个分支各有大标题 + 说明 + 按钮组；补回「暂不更新」「稍后重启」；**下载给了真进度条**（后端有 `got`/`total`，原先只写个百分比数字）。设置页只留入口 + 版本号 |

**E13 没做的**：线路表的 radio + 「测下载速度」（报告 #69）。那需要后端支持指定线路和实测速度，云端两样都没有（`updateStart` 永远发 `line: ''`）。**只搬形态，不编功能。**

### 测试

**65 → 85 条**，后端 115 条不变。动作 41/41 零漏接，五个页面（main / history / settings / about / update），四个源文件仍是纯 LF。

改过的测试都记在案，**没有一条是放宽**：

| 原测试 | 为什么改 |
|---|---|
| 体检回来时这批已经转完了 | 构造与自身注释矛盾，锁的是 A1 那个 bug 行为 → 改成真实时序，另加 2 条钉死修复 |
| 列出文件、页数、勾选框 | 断言 `data-check` 属性名 → 改成断言「整行可点」行为，更强 |
| 一份都没勾就不给开始 | 三态下 `picked={}` 是全选 → 拆成「默认全勾能开始」+「显式 false 才禁用」 |
| 转完给每份的结果 | 断言旧文案 `1 成 / 1 份` → 新文案 + 锁住统计在底栏 |
| 没历史时给一句话 | 断言旧文案 → 新文案 + 副标题 |
| 轮询撞到任务不存在时要停下来 | 硬截 1600 字符找「404」，poll 变长就误报 → 改成截到下一个同级函数 |
| 6 条更新相关 | 更新搬到独立页 → 改渲染 `update` 页，并加严断言标题层级 |

### ⚠️ 一次都没真机验证

全部结论来自代码与测试。**新增或大改的屏**：关于页、连不上后台屏、拦截屏、日志屏、
更新整屏（10 个分支）、待转清单（表头/全选/整行可点）、底栏、报告页、历史页。
